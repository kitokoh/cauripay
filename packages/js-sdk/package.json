{
  "name": "@goursi/js-sdk",
  "version": "0.1.0",
  "private": true,
  "description": "SDK officiel TypeScript GOURSI (Node + navigateur) : payments, webhooks.verifySignature.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "jest",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint \"src/**/*.ts\" --max-warnings 0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^22.10.1",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^7.0.2"
  },
  "engines": { "node": ">=20.0.0" }
}
