name: goursi_flutter
description: "SDK officiel GOURSI (CauriPay) pour Flutter — paiements, webhooks signés, sandbox."
version: 0.1.0
homepage: https://github.com/kitokoh/cauripay
repository: https://github.com/kitokoh/cauripay
environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  crypto: ^3.0.3
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
